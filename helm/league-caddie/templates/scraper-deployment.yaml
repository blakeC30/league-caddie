---
# Scraper Deployment — APScheduler background process.
#
# Runs ESPN sync jobs on a time-based schedule (see app/scraper_main.py):
#   - Every 5 min: live score sync (during active tournaments)
#   - Daily 06:00 UTC: schedule sync (upserts tournaments, fires TOURNAMENT_COMPLETED events)
#   - Daily 14:00 / 18:00 / 11:00 UTC: golfer field sync (2 days / 1 day / same day before start)
#   - Daily 09:00, 15:00, 21:00 UTC: results finalization safety net
#
# No HTTP port — this pod only writes to Postgres and publishes SQS events.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "league-caddie.fullname" . }}-scraper
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "league-caddie.labels" . | nindent 4 }}
    app.kubernetes.io/component: scraper
spec:
  replicas: {{ .Values.scraper.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
  selector:
    matchLabels:
      {{- include "league-caddie.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: scraper
  template:
    metadata:
      labels:
        {{- include "league-caddie.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: scraper
      annotations:
        rollme: {{ randAlphaNum 5 | quote }}
    spec:
      {{- if .Values.image.registry }}
      imagePullSecrets:
        - name: ecr-registry
      {{- end }}
      containers:
        - name: scraper
          image: {{ include "league-caddie.image" (dict "root" . "repo" .Values.scraper.image.repository) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "league-caddie.secretName" . }}
                  key: POSTGRES_PASSWORD
            - name: DATABASE_URL
              value: "postgresql://{{ .Values.postgres.user }}:$(POSTGRES_PASSWORD)@postgres:5432/{{ .Values.postgres.database }}"
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "league-caddie.secretName" . }}
                  key: SECRET_KEY
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "league-caddie.secretName" . }}
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "league-caddie.secretName" . }}
                  key: AWS_SECRET_ACCESS_KEY
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "league-caddie.configmapName" . }}
                  key: ENVIRONMENT
            - name: AWS_REGION
              valueFrom:
                configMapKeyRef:
                  name: {{ include "league-caddie.configmapName" . }}
                  key: AWS_REGION
            - name: AWS_ENDPOINT_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "league-caddie.configmapName" . }}
                  key: AWS_ENDPOINT_URL
            - name: SQS_QUEUE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "league-caddie.configmapName" . }}
                  key: SQS_QUEUE_URL
          resources:
            {{- toYaml .Values.scraper.resources | nindent 12 }}
